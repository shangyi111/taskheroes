export interface Message {
    id: string;
    chatroomId: string;
    senderId: string;
    text: string;
    createdAt: Date;
    updatedAt: Date;
    // Add other relevant properties as needed
  }
  